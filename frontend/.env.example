# ShieldLend frontend environment variables
# Copy to .env and fill in values after running: npm run deploy-all:sepolia

# ConfidentialCreditScore contract address (set after deploying)
VITE_SCORE_CONTRACT_ADDRESS=
